import React from 'react';
import logo from '.././logo.svg';

export const Header = () => (
  <header className="App-header">
    <h1 className="App-title">Your Next Trip</h1>
    <img src={logo} className="App-logo" alt="logo" />
  </header>
);